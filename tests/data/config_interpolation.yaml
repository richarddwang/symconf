# Test configuration with interpolation
dataset:
  name: cifar10
  num_classes: ${BASE_FEATURE_SIZE}

model:
  output_features: ${dataset.num_classes}
  name: model_${dataset.name}_v2
  hidden_dim: ${FEATURE_SIZE}
  dropout: ${0.1 if 10 < 5 else 0.0}

total_params: ${10 + 10}